# 빌드 산출물
build/
.gradle/
bin/

# IDE 개인 설정
.idea/
*.iml
out/

# 로컬 DB 파일 (H2) — 절대 커밋 금지
/data/
backend/data/

# 로그
*.log

# OS
.DS_Store

# 환경 변수·비밀값 (.env.prod 등 — 예시 파일만 커밋)
.env
.env.*
!.env.*.example
# 운영 시딩 파일 폴더 (docker-compose.prod.yml 이 ./seed 를 마운트)
/seed/
# 행사 시딩 파일 — 계정 정보가 들어가므로 예시 파일만 커밋한다
backend/seed/*.json
*.seed.json
!**/event-seed.example.json
*.pem
*.key
